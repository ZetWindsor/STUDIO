/* let isSunnyDay = true
let isHomeworkCompleted = false

let str = isSunnyDay && isHomeworkCompleted ? "jesse can goes out to play"  : "jesse stay home"

console.log(str)
 */



let isSunnyDay = true
let isHomeworkCompleted = true

if(isSunnyDay && !isHomeworkCompleted){
  console.log( "jesse can play")
}
else{
  console.log( "jesse stays home")
}